import React from 'react';
import Counter from './../containers/counter';
import LoginPage from './../containers/login';

export const Home = () => {
  return (
    <React.Fragment>
      <LoginPage />
    </React.Fragment>
  );
};
